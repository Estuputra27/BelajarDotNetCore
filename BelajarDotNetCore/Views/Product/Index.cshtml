@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model IEnumerable<BelajarDotNetCore.Models.Product>
@section Styles {
    <link rel="stylesheet" href="~/css/product.css" />
}

    <div class="text-center">
        <h1 class="display-4">Welcome</h1>
        <p>This is index page of product!</p>
    <div class="d-flex justify-content-center mb-4 navku">
        <a asp-area="" asp-controller="Product" asp-action="Create"class ="btn btn-dark" role="button">Create Product</a>
    </div>
    </div>

        <table class="table table-hover">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.ProductId)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ProductName)
                </th>
            </tr>
            @foreach (var item in Model)
            {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProductId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProductName)
            </td>
        </tr>
            }

        </table>
    